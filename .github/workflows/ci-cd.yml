name: CI/CD

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Build Docker Image
      run: docker build -t ahmedyaar/my-image:latest .

    - name: Login to DockerHub
      run: docker login -u ${{ ahmedyaar }} -p ${{ dckr_pat_s12gjfyogSk363PtOfOjLnYS7Fs }}

    - name: Push to DockerHub
      run: docker push ahmedyaar/my-image:latest
